import { useSelector, useDispatch } from "react-redux";

export function useMonkeezForAddress(address) {
  //   const account = useSelector((state) => state.account.value);
  // get Monkeez for the owned address

  return account;
}
